services:
  api:
    build:
      context: ./api
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - api

  worker:
    build:
      context: ./worker
    depends_on:
      - api
